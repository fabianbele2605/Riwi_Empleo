/**
 * Índice de exception filters de la aplicación
 * 
 * Filters disponibles:
 * - GlobalExceptionFilter: Manejo consistente de todas las excepciones
 */

export { GlobalExceptionFilter } from './global-exception.filter';